Class Application

    ' Application-level events, such as Startup, Exit, and DispatcherUnhandledException
    ' can be handled in this file.


    Protected Overrides Sub OnStartup(e As StartupEventArgs)

        'Dim conf As New CloneMakerConfiguration

        'conf.Replaces = New System.Collections.ObjectModel.ObservableCollection(Of Replace)


        
        'conf.Replaces.Add(New Replace("disso", "pra isso"))
        'conf.Replaces.Add(New Replace("disso", "pra isso"))

        'CloneMakerConfiguration.SaveInXml(conf, "c:\__xml.xml")

        'conf = CloneMakerConfiguration.LoadFromXml("c:\__xml.xml")

        'Stop

    End Sub



End Class

